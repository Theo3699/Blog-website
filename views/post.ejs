<%- include("partials/header"); -%>

<% allInfo.forEach(function(element) { %>
    <% if (element.title == ejsTitle) { %>
        <h1>   <%= element.title %> </h1>
    
        <p> <%= element.content %> </p>
    <% } %>
<% }); %>

<%- include("partials/footer"); -%>