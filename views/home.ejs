<%- include("partials/header"); -%>

<h1> <%= htmlHome %> </h1>

<p> <%= htmlParagraph %> </p>

<% allInfo.forEach(function(element) { %>
    <h1>   <%= element.title %> </h1>
    
    <p>
        <%= element.body.substring(0, 100).concat("", "...") %> 
        <a href="/posts/<%= element.title %>">Read more</a>
    </p> 
<% }); %>

<%- include("partials/footer"); -%>